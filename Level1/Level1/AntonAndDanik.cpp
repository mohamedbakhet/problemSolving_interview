/*
 #include<iostream>
using namespace std;
int main() {
	int n;
	cin >> n;
	int sumA = 0;
	int sumD = 0;

	string s;
	cin >> s;
	for (int i = 0; i < s.length(); i++)
	{
		
		if (s[i] == 'A')
			sumA++;
		else if(s[i] == 'D')
			sumD ++;
	}
	if (sumA > sumD)
		cout << "Anton";
	else if (sumA < sumD)
		cout << "Danik";
	else
		cout << "Friendship";
	return 0;
}
*/
